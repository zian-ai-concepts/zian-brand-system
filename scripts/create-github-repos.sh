#!/usr/bin/env bash
set -euo pipefail

# Optional helper. Requires GitHub CLI and an authenticated session: gh auth login
# Run from the root of this starter kit.

ORG="zian-ai-concepts"

echo "Create repositories manually or uncomment the commands below after review."

# gh repo create "$ORG/.github" --public --source "repositories/.github" --remote github --push
# gh repo create "$ORG/zian-brand-system" --private --source "repositories/zian-brand-system" --remote github --push
